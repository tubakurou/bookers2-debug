<!--id="comments_areaをrenderで指定した部分テンプレートに更新-->
$("#comments_area").html("<%= j(render 'book_comments/book_comments', book: @book, comment: @comment) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');